{
  "name": "www.marcwrobel.fr",
  "version": "1.0.0",
  "description": "Site Web www.marcwrobel.fr.",
  "private": true,
  "scripts": {
    "clean": "rm -rf node_modules _site",
    "install": "bundle install && npm install",
    "update": "bundle update && npm update && webpack && cp -v node_modules/bootstrap/dist/css/bootstrap.min.css _sass/bootstrap.scss",
    "jekyll": "bundle exec jekyll"
  },
  "dependencies": {
    "bootstrap": "^4.4.1",
    "jquery": "^3.4.1",
    "popper.js": "^1.16.0"
  },
  "devDependencies": {
    "webpack": "^4.41.2",
    "webpack-cli": "^3.3.10"
  }
}
